<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.blueHouse.mapper.ProjectMapper">
    <select id="findProjectById" parameterType="string" resultType="com.blueHouse.pojo.orders.Project">
        select id,name,url,enabled,category from t_projects where id = #{id}
    </select>
    <insert id="insertProject" parameterType="com.blueHouse.pojo.orders.Project">
        insert into t_projects(id,name,url,enabled,category)  values(#{id}, #{name}, #{url}, #{enabled},#{category})
    </insert>
    <update id="updateProject" parameterType="com.blueHouse.pojo.orders.Project">
    </update>
</mapper>